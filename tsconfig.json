{
  // this file is just for this repo itself,
  // it's not exposed to consumers.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "rootDir": null,
    "module": "node16",
    "emitDeclarationOnly": true,
    "noEmit": false,
    "types": ["node"],
    "outDir": "types"
  },
  "exclude": ["tests", "types"]
}
